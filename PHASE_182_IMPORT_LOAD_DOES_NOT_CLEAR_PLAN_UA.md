phase182

Що виправлено:
- завантаження нового Excel/CSV/URL більше не скидає поточний план і таблиці ще ДО натискання кнопки застосування
- причина була в тому, що import loaders викликали resetImportedState() одразу після читання файлу
- тепер file/url loader лише оновлює import source (headers/rows/mapping), але не чистить players, shiftPlans, tower snapshot і final plan

Очікувана поведінка:
1. завантажив новий файл
2. старий фінальний план поки лишається як є
3. натиснув "Оновити склад + зберегти план"
4. тільки тоді оновлюється roster без скидання плану
